<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Geocoder: turning a GPS fix into a city name is a network call. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <!-- Notifications: prayer reminders + the pinned Prayer Focus notification. Runtime-requested on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Drive backup runs as a WorkManager foreground job (a quiet progress notification) so the upload is not cut short. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Prayer Focus: needed to set the ringer to Silent (the Vibrate mode needs nothing). Granted in Settings. -->
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />
    <!-- Prayer Focus: let the user exempt the app from battery optimization so it keeps working when closed. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS"
        tools:ignore="BatteryLife" />
    <!-- Prayer Focus: exact alarms mute at the slot and restore at its end. -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Prayer Focus: re-arm the prayer alarms after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
        android:name=".NoorApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoorIslam.Splash">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Widget: the customize sheet, shown over the home screen when a widget is dropped. Translucent so the
             bottom sheet floats over the dimmed home screen. -->
        <activity android:name=".PrayerCardConfigActivity" android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Prayer Focus: alarm fires at the slot, mutes, and pins the notification. -->
        <receiver
            android:name="com.kodeelite.nooreislam.core.focus.FocusAlarmReceiver"
            android:exported="false" />
        <!-- Prayer Focus: the notification's action buttons (+5 / prayed / unmute) and the end "restore" alarm. -->
        <receiver
            android:name="com.kodeelite.nooreislam.core.focus.FocusActionReceiver"
            android:exported="false" />
        <!-- Prayer Focus: re-arm when the clock or timezone changes (armed alarms target stale instants). System-sent. -->
        <receiver
            android:name="com.kodeelite.nooreislam.core.focus.TimeChangeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>
        <!-- Prayer Focus: nightly re-arm. -->
        <receiver
            android:name="com.kodeelite.nooreislam.core.focus.RescheduleReceiver"
            android:exported="false" />
        <!-- Prayer Focus: re-arm after reboot or an app update (both wipe scheduled alarms). System-sent. -->
        <receiver
            android:name="com.kodeelite.nooreislam.core.focus.BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Notifications: posts a prayer/dhikr/surah reminder when its alarm fires. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationReceiver"
            android:exported="false" />
        <!-- Notifications: silent nightly wake to roll the reminder window forward. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationRebuildReceiver"
            android:exported="false" />
        <!-- Notifications: re-arm when the clock or timezone changes. System-sent. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationTimeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>
        <!-- Notifications: re-arm after reboot or app update (both wipe scheduled alarms). System-sent. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Widget: minimal 2×2 (design 2c). -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.MinimalWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_minimal_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/minimal_widget_info" />
        </receiver>
        <!-- Widget: dark prayer card (design 8b) — current prayer + 5 times. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.PrayerCardWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_card_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/prayer_card_widget_info" />
        </receiver>
        <!-- Widget: prayer times (current + next + slot strip), 1–2 rows. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.PrayerTimesWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_times_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/prayer_times_widget_info" />
        </receiver>
        <!-- Widget: bar 4×1 (design 23e). -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.PrayerBarWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_bar_name">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/prayer_bar_widget_info" />
        </receiver>
        <!-- Widget: current prayer 2×2 (design 19d). -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.PrayerNextWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_next_name">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/prayer_next_widget_info" />
        </receiver>
        <!-- Widget: tile 2×2, Miqat icon (design 19f). -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.PrayerTileWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_tile_name">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/prayer_tile_widget_info" />
        </receiver>
        <!-- Widget: icon 1×1 round app-icon badge, time-first (design 19f). -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.PrayerIconWidgetReceiver"
            android:exported="true"
            android:label="@string/widget_icon_name">
            <intent-filter><action android:name="android.appwidget.action.APPWIDGET_UPDATE" /></intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/prayer_icon_widget_info" />
        </receiver>
        <!-- Widget: fires at each prayer transition + every ~15 min to redraw the countdown. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.widget.WidgetAlarmReceiver"
            android:exported="false" />


        <!-- WorkManager's foreground service must carry the type the backup job uses, or Android refuses to start it. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
