<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Notifications: Surah Al-Mulk / Al-Kahf reminders. Runtime-requested on Android 13+. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Notifications: exact alarm wakes for the reminder time. User-granted via the in-app prompt
         (ExactAlarmNeedsAttention) — no USE_EXACT_ALARM, which Play restricts to alarm/calendar apps. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <!-- Notifications: re-arm the alarms after a reboot. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Notifications: one-tap battery exemption dialog, so OEMs (Vivo/Xiaomi) can't freeze the
         app and defer its reminder alarms. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <!-- Notifications: one-tap battery exemption dialog, so OEMs (Vivo/Xiaomi) can't freeze the
         app and defer its reminder alarms. -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <application
        android:name=".QuranApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.NoorQuran.Splash">
        <activity
            android:exported="true"
            android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Notifications: posts a Surah Al-Mulk/Al-Kahf reminder when its alarm fires. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationReceiver"
            android:exported="false" />
        <!-- Notifications: silent nightly wake to roll the reminder window forward. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationRebuildReceiver"
            android:exported="false" />
        <!-- Notifications: re-arm when the clock or timezone changes. System-sent. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationTimeReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
            </intent-filter>
        </receiver>
        <!-- Notifications: re-arm after reboot or app update (both wipe scheduled alarms). System-sent. -->
        <receiver
            android:name="com.kodeelite.nooreislam.feature.notifications.scheduler.NotificationBootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Studio: hands the generated ayah image to the share sheet as a content:// uri. -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>
